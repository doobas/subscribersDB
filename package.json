{
  "name": "mailerlite",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "serve": "cd resources/frontend && yarn run serve",
    "build": "cd resources/frontend && yarn run build",
    "lint": "cd resources/frontend && yarn run lint",
    "test": "cd resources/frontend && yarn run test:unit"
  }
}